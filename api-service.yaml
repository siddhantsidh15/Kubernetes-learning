apiVersion: v1
kind: Service
metadata:
  name: api-service
  namespace: default
spec:
  ports:
    - port: 80
      protocol: TCP
      targetPort: 8080
      nodePort: 30080
  selector:
    app: synergychat-api
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
